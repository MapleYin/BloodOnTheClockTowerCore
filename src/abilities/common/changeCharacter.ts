import { CharacterForKey } from '../../characters';
import { hasRealAbility } from '../../common';
import { AliveAtNight } from '../helper';

export const ChangeCharacter: BCT.TAbility = {
    key: "ChangeCharacter",
    effect: (operation, players) => {
        
    }
}